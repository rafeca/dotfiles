# initialize autocomplete here, otherwise functions won't be loaded

source "$(dirname $0)/path.zsh"
source "$(dirname $0)/config.zsh"
source "$(dirname $0)/prompt.zsh"
source "$(dirname $0)/aliases.zsh"
source "$(dirname $0)/folders.zsh"
